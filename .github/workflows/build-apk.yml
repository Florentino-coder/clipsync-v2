name: Build Release Files

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Create Flutter Android project
        run: |
          flutter create --platforms=android --org com.clipsync mobile_build
          cp mobile/pubspec.yaml mobile_build/pubspec.yaml
          rm -rf mobile_build/lib
          cp -R mobile/lib mobile_build/lib
          cp mobile/android/app/src/main/AndroidManifest.xml mobile_build/android/app/src/main/AndroidManifest.xml
          if [ -d mobile/android/app/src/main/res ]; then
            cp -R mobile/android/app/src/main/res/* mobile_build/android/app/src/main/res/
          fi
          perl -0pi -e 's/\n    ndkVersion = flutter\.ndkVersion\n/\n/' mobile_build/android/app/build.gradle.kts

      - name: Install dependencies
        working-directory: mobile_build
        run: flutter pub get

      - name: Build debug APK
        working-directory: mobile_build
        run: flutter build apk --debug

      - name: Rename APK
        run: |
          mkdir -p artifacts
          cp mobile_build/build/app/outputs/flutter-apk/app-debug.apk artifacts/ClipSync.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ClipSync-APK
          path: artifacts/ClipSync.apk

  build-windows-exe:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pc/requirements.txt

      - name: Build Windows EXE
        working-directory: pc
        shell: pwsh
        run: .\build_exe.ps1

      - name: Prepare EXE artifact
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifacts | Out-Null
          Copy-Item pc\dist\ClipSyncPC.exe artifacts\ClipSyncPC.exe -Force
          Copy-Item pc\clipsync_pc_config.example.json artifacts\clipsync_pc_config.example.json -Force

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: ClipSync-Windows-EXE
          path: |
            artifacts/ClipSyncPC.exe
            artifacts/clipsync_pc_config.example.json

  publish-release:
    runs-on: ubuntu-latest
    needs:
      - build-apk
      - build-windows-exe
    if: github.ref == 'refs/heads/master'
    env:
      PUBLIC_RELEASE_TOKEN: ${{ secrets.PUBLIC_RELEASE_TOKEN }}

    steps:
      - name: Download APK artifact
        uses: actions/download-artifact@v4
        with:
          name: ClipSync-APK
          path: release

      - name: Download EXE artifact
        uses: actions/download-artifact@v4
        with:
          name: ClipSync-Windows-EXE
          path: release

      - name: Upload files to repository release
        if: github.ref == 'refs/heads/master'
        uses: ncipollo/release-action@v1
        with:
          token: ${{ github.token }}
          tag: android-latest
          name: ClipSync Test Builds
          body: Latest test APK and Windows EXE built from master@${{ github.sha }}.
          artifacts: release/*
          allowUpdates: true
          removeArtifacts: true
          replacesArtifacts: true
          artifactErrorsFailBuild: true

      - name: Upload APK to public release
        if: github.ref == 'refs/heads/master' && env.PUBLIC_RELEASE_TOKEN != ''
        uses: ncipollo/release-action@v1
        with:
          owner: Florentino-coder
          repo: clipsync-apk
          token: ${{ secrets.PUBLIC_RELEASE_TOKEN }}
          tag: android-latest
          name: ClipSync Android APK
          body: Latest debug APK built from Florentino-coder/clipsync@${{ github.sha }}.
          commit: main
          artifacts: release/ClipSync.apk
          allowUpdates: true
          removeArtifacts: true
          replacesArtifacts: true
          artifactErrorsFailBuild: true
