name: clipsync_app
description: Clipboard sync from PC to phone

publish_to: "none"

version: 0.8.3+14

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_foreground_task: 8.17.0
  mobile_scanner: 5.2.3
  shared_preferences: 2.3.2
  url_launcher: 6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
